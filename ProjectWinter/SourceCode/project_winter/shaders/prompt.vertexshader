#version 330 core

layout(location = 0) in vec3 vertexPosition;
layout(location = 2) in vec2 vertexUV; // location = 2 για τα UVs!

out vec2 uv;

void main()
{
    // Η OpenGL χρησιμοποιεί κανόνα αριστερού χεριού για το Clip Space!
    gl_Position = vec4(vertexPosition, 1.0);

    uv = vertexUV;
}
